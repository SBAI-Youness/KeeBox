#pragma once

// Application window title
#define WINDOW_TITLE "KeeBox"

// Default window size settings
#define DEFAULT_WINDOW_WIDTH  800  // Initial width of the window
#define DEFAULT_WINDOW_HEIGHT 800  // Initial height of the window
